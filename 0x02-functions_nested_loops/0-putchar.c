#include "main.h"

/**
 * main - Prints the string _putchar using method _putchar.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "_putchar";
	int ch;

	for (ch = 0; ch < 8; ch++)
	{
		_putchar(ch[i]);
	}
	_putchar('\n');
	return (0);
}
