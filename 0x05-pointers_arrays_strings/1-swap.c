/**
 * swap_int - function that swaps the value of 2 integers
 * @a: one input pointer to integer
 * @b: another input pointer to integer input to swap
 * Return: nothing
*/
void swap_int(int *a, int *b)
{
	int c = *a
	*a = *b;
	*b = c;


}
